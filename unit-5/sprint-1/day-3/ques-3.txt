##Indexes
->We create indexes when our main intension is retrieving data from the table or a Column of a table,
 It just reduce the time of the operation, i.e, noraml seaerchig operation use extensive amount of time,
but, Indexing reducces the operation time, It just gives memory location to the tuples.

create index indx_name ON Students(Name);